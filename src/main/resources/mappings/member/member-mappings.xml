<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="MemberDAO">

	<resultMap type="member" id="member">
	   <!--<id property="seq" column="SEQ" />  --> 
		<id property="member_id" column="MEMBER_ID" />
		<result property="member_no" column="MEMBER_NO" />
		<result property="member_pwd" column="MEMBER_PWD" />
		<result property="member_name" column="MEMBER_NAME" />
		<result property="member_phone" column="MEMBER_PHONE" />
		<result property="member_age" column="MEMBER_AGE" />
		<result property="member_email" column="MEMBER_EMAIL" />
		<result property="member_addr" column="MEMBER_ADDR" />
		<result property="member_key" column="MEMBER_KEY" />
		<result property="member_date" column="MEMBER_DATE" />
	</resultMap>
	
	<select id="checkID" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM MEMBER
		WHERE MEMBER_ID=#{member_id}
	</select>

	<select id="checkMember" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM MEMBER
		WHERE MEMBER_ID=#{member_id} AND MEMBER_PWD=#{member_pwd}
	</select>
	
	<!-- 회원가입 -->
	<insert id="insertMember" parameterType="myapp.model.member.vo.MemberVO">
		<![CDATA[
		INSERT INTO MEMBER (
			MEMBER_SEQ, 
			MEMBER_NO, 
			MEMBER_ID, 
			MEMBER_PWD, 
			MEMBER_NAME, 
			MEMBER_PHONE, 
			MEMBER_AGE, 
			MEMBER_EMAIL,
			MEMBER_ADDR,
			MEMBER_KEY,
			MEMBER_DATE
			
		) VALUES(
			SEQ.NEXTVAL, 
			#{member_id}, 
			#{member_pwd}, 
			#{member_name}, 
			#{member_phone},
			#{member_age},
			#{member_email},
			#{member_addr},
			#{member_key},
			DATE(#{member_date}, 'yyyy-mm-dd')
			
		)
		]]>
	</insert>
	<!-- 회원정보 수정 -->
	<update id="updateMember">
		UPDATE MEMBER
		SET
		MEMBER_PWD = #{member_pwd},
		MEMBER_EMAIL = #{member_email},
		MEMBER_PHONE = #{member_phone},
		MEMBER_ADDR = #{member_ADDR},
		WHERE
		MEMBER_ID = #{member_id}
	</update>
	
	<select id="login" resultType ="myapp.model.member.vo.MemberVO">
	
			SELECT * FROM MEMBER
			WHERE MEMBER_ID=#{member_id} AND MEMBER_PWD=#{member_pwd}
	
	</select>
	
 	<!-- 회원탈퇴 -->
	<delete id="deleteMember">
		DELETE FROM MEMBER WHERE
		MEMBER_ID = #{member_id}
	</delete>
	<select id="forgotPWChkMember" resultType="java.lang.Integer">
		SELECT COUNT(*) FROM MEMBER WHERE MEMBER_ID=#{member_id} AND MEMBER_NAME=#{member_name}
	</select>
	
	<update id="forgotPWUpdate">
		UPDATE MEMBER SET MEMBER_PWD=#{member_pwd} WHERE MEMBER_ID=#{member_id} AND MEMBER_NAME=#{member_name}
	</update>

</mapper>
